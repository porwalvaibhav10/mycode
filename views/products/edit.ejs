<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h1>Edit this product details here</h1>
    <form action="/products/<%=spproduct._id%>?_method=put" method="post">
        <label for="name">Product name</label>
        <input type="text" id="name" name="name" placeholder="product name" value="<%=spproduct.name%>">
        <br>
        <label for="price">price (unit)</label>
        <input type="number" id="price" name="price" placeholder="price"  value="<%=spproduct.price%>">
        <br>
        <label for="category">Select Category </label>
        <select name="category" id="category" value="<%=spproduct.category%>">
            <% for(let category of categories) { %>
                <option value="<%=category%>"<%= spproduct.category === category?'selected': ''%> ><%=category%></option>
            <% }%>
        </select>
        <button>Submit</button>
        <a href="/products/<%=spproduct._id%>"><button>cancel</button></a>
    </form>
</body>
</html>